# Normalize line endings across the team (Windows/macOS/Linux).
# Prevents CRLF/LF diff noise in pull requests.
* text=auto eol=lf

# Binary files — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
